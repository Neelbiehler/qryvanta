# Changesets

Use changesets to version and publish reusable packages.

## Creating a release note

```bash
pnpm changeset
```

Select the package(s) and bump type.

## Applying version bumps

```bash
pnpm changeset:version
```

## Publishing

```bash
pnpm changeset:publish
```
